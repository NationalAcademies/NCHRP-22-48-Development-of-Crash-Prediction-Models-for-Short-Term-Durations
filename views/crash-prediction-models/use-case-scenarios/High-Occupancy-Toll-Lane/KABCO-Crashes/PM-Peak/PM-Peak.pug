
extends ../../../../../base.pug
block content
    div
        ul(class="list")
            li
                h2 Special Model (FL)
                p Predicted Crashes=No.of years ×Segment Length                             ×exp⁡(-8.795 +1.652 Ln (Total Volume)-1.572 Ln (AvgSpeed_GP )+0.155 Diff.of Avg.Occupancy (Downstream_GP-Target_GP) +1.448 HOT Exit).


        form(method="post", action="/figure", enctype="multipart/form-data")
            p
                span Select data file:
                input(type="file" name="dataFile")

            p
                button(type="submit" class="btn btn-primary")  Upload
